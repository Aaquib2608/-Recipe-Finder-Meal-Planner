# Recipe App (SvelteKit)

README for running and understanding this project.

## Setup Instructions

1. Install Node.js (recommended: latest LTS).
2. Install dependencies:

```sh
npm install
```

3. Optional checks:

```sh
npm run check
npm run lint
```

## Assumptions Made

- Node.js and npm are already installed on your machine.
- This project uses the existing scripts in package.json (vite + svelte-kit).
- The GitHub repository URL is inferred because no git remote is configured in this workspace.

## Starting Development Server

Run:

```sh
npm run dev
```

Open the local URL shown in your terminal (typically http://localhost:5173).

## Stencil Library (Published on npm)

- https://www.npmjs.com/package/@mdaaquibkhan/recipe-ui-components

## GitHub Repository

- https://github.com/mdaaquibkhan/aaquibkhan

If your actual repository URL is different, replace the link above.
